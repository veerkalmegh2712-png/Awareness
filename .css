/* ===========================
   InAmigos Foundation Styles
   =========================== */

* { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }

body {
  font-family: 'Poppins', sans-serif;
  color: #2d2d2d;
  background: #fff;
  line-height: 1.6;
}

img { max-width: 100%; display: block; }
a { text-decoration: none; }
p { margin-bottom: .9rem; color: #555; }
p:last-child { margin-bottom: 0; }

.container {
  max-width: 1160px;
  margin: 0 auto;
  padding: 0 1.5rem;
}

.section { padding: 5rem 0; }
.bg-light { background: #f7f9fc; }

/* ── BUTTONS ── */
.btn-orange {
  background: #E8530E;
  color: #fff;
  padding: .75rem 1.8rem;
  border-radius: 4px;
  font-weight: 600;
  font-size: .95rem;
  display: inline-block;
  transition: background .2s, transform .2s;
}
.btn-orange:hover { background: #C94309; transform: translateY(-2px); }

.btn-white {
  background: #fff;
  color: #E8530E;
  padding: .75rem 1.8rem;
  border-radius: 4px;
  font-weight: 600;
  font-size: .95rem;
  display: inline-block;
  transition: opacity .2s, transform .2s;
  border: 2px solid #fff;
}
.btn-white:hover { opacity: .9; transform: translateY(-2px); }

.btn-outline {
  border: 2px solid #E8530E;
  color: #E8530E;
  padding: .7rem 1.8rem;
  border-radius: 4px;
  font-weight: 600;
  font-size: .95rem;
  display: inline-block;
  transition: background .2s, color .2s;
}
.btn-outline:hover { background: #E8530E; color: #fff; }

/* ── SECTION HEADERS ── */
.section-tag {
  font-size: .78rem;
  font-weight: 700;
  letter-spacing: 2.5px;
  text-transform: uppercase;
  color: #E8530E;
  margin-bottom: .5rem;
}
.section-title {
  font-size: 2rem;
  font-weight: 800;
  color: #1a1a2e;
  line-height: 1.2;
  margin-bottom: .75rem;
}
.section-sub {
  color: #666;
  font-size: 1rem;
  max-width: 540px;
}
.section-head { text-align: center; margin-bottom: 3rem; }
.section-head .section-sub { margin: 0 auto; }

/* ── TOP BAR ── */
.topbar {
  background: #1a1a2e;
  color: rgba(255,255,255,.7);
  font-size: .8rem;
  padding: .45rem 5%;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 2rem;
}
.topbar span { display: flex; align-items: center; gap: .4rem; }
.topbar-social { display: flex; gap: .6rem; }
.topbar-social a {
  color: rgba(255,255,255,.6);
  width: 24px; height: 24px;
  background: rgba(255,255,255,.1);
  border-radius: 50%;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: .75rem; font-weight: 700;
  transition: background .2s;
}
.topbar-social a:hover { background: #E8530E; color: #fff; }

/* ── NAVBAR ── */
.navbar {
  background: #fff;
  box-shadow: 0 2px 12px rgba(0,0,0,.08);
  position: sticky;
  top: 0;
  z-index: 99;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 5%;
}
.nav-logo {
  display: flex; align-items: center; gap: .75rem;
}
.logo-icon { font-size: 2rem; }
.nav-logo strong {
  display: block; font-size: 1.05rem; font-weight: 800;
  color: #1a1a2e; line-height: 1.1;
}
.nav-logo small { font-size: .7rem; color: #888; }
.nav-links {
  display: flex; gap: 2rem; list-style: none;
}
.nav-links a {
  color: #444; font-size: .9rem; font-weight: 500;
  transition: color .2s;
}
.nav-links a:hover { color: #E8530E; }
.nav-donate {
  background: #E8530E; color: #fff;
  padding: .55rem 1.4rem; border-radius: 4px;
  font-weight: 600; font-size: .88rem;
  transition: background .2s;
}
.nav-donate:hover { background: #C94309; }

/* ── HERO ── */
.hero {
  min-height: 88vh;
  background: url('https://images.unsplash.com/photo-1593113598332-cd288d649433?w=1400&q=80') center/cover no-repeat;
  position: relative;
  display: flex;
  align-items: center;
  padding: 5rem 5%;
}
.hero-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(to right, rgba(26,26,46,.88) 50%, rgba(26,26,46,.4));
}
.hero-content {
  position: relative; z-index: 1;
  max-width: 620px;
}
.hero-tag {
  font-size: .78rem; font-weight: 700; letter-spacing: 2px;
  text-transform: uppercase; color: #ffb347;
  margin-bottom: 1rem;
}
.hero h1 {
  font-size: clamp(2.2rem, 5vw, 3.8rem);
  font-weight: 800;
  color: #fff; line-height: 1.1;
  margin-bottom: 1.2rem;
}
.hero h1 span { color: #E8530E; }
.hero-sub {
  color: rgba(255,255,255,.75);
  font-size: 1.05rem; line-height: 1.7;
  margin-bottom: 2rem;
}
.hero-btns { display: flex; gap: 1rem; flex-wrap: wrap; }

/* ── STATS BAR ── */
.stats-bar {
  background: #E8530E;
  display: flex; justify-content: center; flex-wrap: wrap;
  gap: 0;
}
.stat-item {
  padding: 1.4rem 2.5rem;
  text-align: center;
  border-right: 1px solid rgba(255,255,255,.2);
}
.stat-item:last-child { border-right: none; }
.stat-item strong {
  display: block; font-size: 1.7rem; font-weight: 800;
  color: #fff;
}
.stat-item span { font-size: .78rem; color: rgba(255,255,255,.85); text-transform: uppercase; letter-spacing: 1px; }

/* ── ABOUT ── */
.about-grid {
  display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center;
}
.about-img-wrap {
  border-radius: 8px; overflow: hidden;
  position: relative; background: #eee;
  min-height: 380px;
}
.about-img-wrap img { width: 100%; height: 100%; object-fit: cover; }
.img-fallback {
  background: linear-gradient(135deg, #1a1a2e, #0F3460);
  display: flex; align-items: center; justify-content: center;
}
.about-badge {
  position: absolute; bottom: 1.5rem; left: 1.5rem;
  background: #E8530E; color: #fff;
  padding: .8rem 1.2rem; border-radius: 6px;
}
.about-badge strong { display: block; font-size: 1.2rem; font-weight: 800; }
.about-badge span { font-size: .78rem; }
.creds-grid {
  display: grid; grid-template-columns: 1fr 1fr; gap: .6rem; margin-top: 1.4rem;
}
.cred-item {
  background: #f7f9fc;
  border: 1px solid #e2e8f0;
  border-left: 3px solid #E8530E;
  padding: .5rem .9rem;
  font-size: .84rem; font-weight: 500; color: #444;
  border-radius: 0 4px 4px 0;
}

/* ── PROJECTS ── */
.projects-grid {
  display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem;
}
.project-card {
  background: #fff;
  border-radius: 8px;
  border: 1px solid #e8edf2;
  overflow: hidden;
  transition: box-shadow .25s, transform .25s;
}
.project-card:hover { box-shadow: 0 8px 30px rgba(0,0,0,.1); transform: translateY(-5px); }
.card-icon {
  height: 100px;
  display: flex; align-items: center; justify-content: center;
  font-size: 2.8rem;
}
.card-body { padding: 1.3rem 1.5rem 1.6rem; }
.card-cat {
  font-size: .72rem; font-weight: 700; letter-spacing: 1.5px;
  text-transform: uppercase; display: block; margin-bottom: .4rem;
}
.card-body h3 { font-size: 1.1rem; font-weight: 700; color: #1a1a2e; margin-bottom: .6rem; }
.card-body p { font-size: .88rem; line-height: 1.65; margin-bottom: 1rem; }
.card-impact {
  background: #f7f9fc;
  border-left: 3px solid;
  padding: .5rem .8rem;
  font-size: .82rem; color: #555;
  border-radius: 0 4px 4px 0;
}
.card-impact strong { font-size: 1rem; }

/* ── IMPACT ── */
.impact { background: linear-gradient(135deg, #1a1a2e 0%, #0F3460 100%); }
.impact-grid {
  display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem;
}
.impact-card {
  background: rgba(255,255,255,.07);
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 8px; padding: 2rem 1.5rem;
  text-align: center;
  transition: background .2s;
}
.impact-card:hover { background: rgba(232,83,14,.18); }
.impact-icon { font-size: 2.5rem; margin-bottom: .8rem; }
.impact-card h3 { font-size: 1.05rem; font-weight: 700; color: #fff; margin-bottom: .6rem; }
.impact-card p { font-size: .87rem; color: rgba(255,255,255,.6); line-height: 1.6; margin: 0; }

/* ── GALLERY ── */
.gallery-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: auto auto;
  gap: 1rem;
}
.gallery-item {
  border-radius: 8px; overflow: hidden;
  position: relative; background: #dde;
  min-height: 200px;
}
.gallery-item img { width: 100%; height: 200px; object-fit: cover; }
.gallery-item:first-child { grid-column: span 2; }
.gallery-item:first-child img { height: 260px; }
.gallery-overlay {
  position: absolute; inset: 0;
  background: rgba(26,26,46,.5);
  display: flex; align-items: flex-end; padding: 1rem;
  opacity: 0; transition: opacity .2s;
}
.gallery-item:hover .gallery-overlay { opacity: 1; }
.gallery-overlay span { color: #fff; font-weight: 600; font-size: .9rem; }
.gallery-fallback { background: #dde; display: flex; align-items: center; justify-content: center; font-size: 3rem; }
.gallery-fallback-static { border-radius: 8px; }

/* ── CTA STRIP ── */
.cta-strip { background: #f7f9fc; padding: 5rem 0; }
.cta-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
.cta-card {
  background: #fff; border-radius: 8px;
  border: 1px solid #e2e8f0;
  padding: 2.5rem 2rem; text-align: center;
  transition: box-shadow .2s, transform .2s;
}
.cta-card:hover { box-shadow: 0 8px 28px rgba(0,0,0,.1); transform: translateY(-4px); }
.cta-card-dark { background: #1a1a2e; border-color: #1a1a2e; }
.cta-card-dark h3 { color: #fff; }
.cta-card-dark p { color: rgba(255,255,255,.65); }
.cta-icon { font-size: 2.8rem; margin-bottom: 1rem; }
.cta-card h3 { font-size: 1.15rem; font-weight: 700; color: #1a1a2e; margin-bottom: .6rem; }
.cta-card p { font-size: .88rem; color: #666; line-height: 1.6; margin-bottom: 1.5rem; }

/* ── FOOTER ── */
footer {
  background: #1a1a2e; color: rgba(255,255,255,.65);
  padding: 4rem 0 0;
}
.footer-grid {
  display: grid; grid-template-columns: 2fr 1fr 1fr 1.5fr; gap: 3rem;
  padding-bottom: 3rem;
  border-bottom: 1px solid rgba(255,255,255,.1);
}
.footer-logo {
  display: flex; gap: .75rem; align-items: center; margin-bottom: 1rem;
}
.footer-logo span { font-size: 2rem; }
.footer-logo strong { display: block; color: #fff; font-size: 1rem; font-weight: 700; }
.footer-logo small { font-size: .7rem; color: rgba(255,255,255,.5); }
.footer-about { font-size: .88rem; line-height: 1.7; color: rgba(255,255,255,.55); }
.footer-social { display: flex; gap: .7rem; margin-top: 1.2rem; }
.footer-social a {
  background: rgba(255,255,255,.08);
  color: rgba(255,255,255,.6);
  padding: .4rem .9rem; border-radius: 4px;
  font-size: .78rem; font-weight: 600;
  transition: background .2s, color .2s;
}
.footer-social a:hover { background: #E8530E; color: #fff; }
.footer-col h4 { color: #fff; font-weight: 700; margin-bottom: 1rem; font-size: .95rem; }
.footer-col ul { list-style: none; }
.footer-col ul li, .footer-col p { font-size: .87rem; color: rgba(255,255,255,.55); margin-bottom: .5rem; line-height: 1.6; }
.footer-col ul li a { color: rgba(255,255,255,.55); transition: color .2s; }
.footer-col ul li a:hover { color: #E8530E; }
.footer-bottom {
  display: flex; justify-content: space-between; align-items: center;
  padding: 1.4rem 0;
  font-size: .82rem; color: rgba(255,255,255,.35);
  flex-wrap: wrap; gap: .5rem;
}
.hashtags { color: rgba(255,255,255,.25); }

/* ── RESPONSIVE ── */
@media (max-width: 1024px) {
  .projects-grid { grid-template-columns: repeat(2, 1fr); }
  .impact-grid { grid-template-columns: repeat(2, 1fr); }
  .footer-grid { grid-template-columns: 1fr 1fr; }
}

@media (max-width: 768px) {
  .navbar { padding: 1rem; }
  .nav-links { display: none; }
  .about-grid { grid-template-columns: 1fr; gap: 2rem; }
  .creds-grid { grid-template-columns: 1fr; }
  .projects-grid { grid-template-columns: 1fr; }
  .cta-grid { grid-template-columns: 1fr; }
  .impact-grid { grid-template-columns: 1fr 1fr; }
  .gallery-grid { grid-template-columns: 1fr 1fr; }
  .gallery-item:first-child { grid-column: span 2; }
  .stats-bar { flex-wrap: wrap; }
  .stat-item { border-right: none; border-bottom: 1px solid rgba(255,255,255,.15); width: 50%; }
  .footer-grid { grid-template-columns: 1fr; gap: 2rem; }
  .footer-bottom { flex-direction: column; text-align: center; }
}
.card-icon {
  width: 100%;
  height: 180px;
  overflow: hidden;
  border-radius: 16px 16px 0 0;
}

.card-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}